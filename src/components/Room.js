import React from 'react';
import propTypes from 'prop-types';

const Room = ({ book, removeBook }) => (
  <div className="Room">
    <p>room goes here</p>
  </div>
);

Room.propTypes = {

};

export default Room;
